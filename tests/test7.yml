macros:
  a: x
  b: 
    - "{{a}}"
    - "y"
  c: 
    - w
    - z

tasks:
  foo:
    command: "echo {{a}} {{u}} {{v}} {{_n}}"
    for:
      var:
        - u
        - v
      in:
        - b
        - c
