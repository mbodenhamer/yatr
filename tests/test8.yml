import:
  - test8.py

macros:
  shebang: "#!/bin/bash"
  bar: "{{env('YATR_BAR', 'baz')}}"
  home: "{{env('PATH')}}"
  baz1: "{{'baz'|foo}}"
  baz2: "{{bar('foo')}}"

tasks:
  foo:
    - echo foo
    - echo bar
    - echo baz

  bar:
    - "echo {{bar}}"

  home:
    - "echo {{home}}"

  baz: "echo {{baz1}} {{baz2}}"
